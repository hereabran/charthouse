# Optional. If unset, Share button falls back to URL-hash payloads (long URLs).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional. Override default helm binary path (defaults to `helm` on PATH).
HELM_BIN=
